<Window x:Class="AddressBookApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AddressBookApp"
        mc:Ignorable="d"
        Title="Адресная книга" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Поле поиска -->
        <TextBox x:Name="SearchBox" Width="200" Margin="0,0,0,10"
                 Text="Поиск..." Foreground="Gray"
                 GotFocus="SearchBox_GotFocus" LostFocus="SearchBox_LostFocus"
                 TextChanged="SearchBox_TextChanged" />

        <!-- Список контактов -->
        <ListView x:Name="ContactsList" Grid.Row="1" Margin="0,0,0,10"
                  DisplayMemberPath="Display" />

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button x:Name="AddButton" Content="Добавить" Margin="5" Click="AddButton_Click"/>
            <Button x:Name="EditButton" Content="Редактировать" Margin="5" Click="EditButton_Click"/>
            <Button x:Name="DeleteButton" Content="Удалить" Margin="5" Click="DeleteButton_Click"/>
            <Button x:Name="ManagePasswordButton" Content="Настроить пароль администратора"
                    Margin="5" Click="ManagePasswordButton_Click" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>
